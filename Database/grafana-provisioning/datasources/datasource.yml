apiVersion: 1
datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    database: ${INFLUXDB_BUCKET}
    user: ${INFLUXDB_USERNAME}
    password: ${INFLUXDB_PASSWORD}
    url: ${INFLUXDB_URL}
    isDefault: true
    editable: true
